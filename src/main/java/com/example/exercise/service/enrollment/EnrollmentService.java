package com.example.exercise.service.enrollment;

import com.example.exercise.entity.Enrollment;
import com.example.exercise.entity.Post;
import com.example.exercise.entity.User;
import java.util.List;

public interface EnrollmentService {

  //참가 신청
//  EnrollmentResponseDto enroll(Long postId,EnrollmentRequestDto enrollmentRequestDto, User user);

  //참가 신청 취소
//  void cancel(Long enrollmentId, Long postId, User user);

  //참여 신청 내역 조회
//  List<EnrollmentResponseDto> getEnrollmentList(Long postId);
}
