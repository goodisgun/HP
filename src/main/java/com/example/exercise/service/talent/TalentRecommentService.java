package com.example.exercise.service.talent;

public interface TalentRecommentService {

  // reComment(대댓글 작성)
//  String reComment(Long commentId, User user, ReCommentRequestDto reCommentRequestDto);

  // updateReComment(대댓글 수정)
  // String updateReComment(Long reCommentId, User user, ReCommentRequestDto reCommentRequestDto);

  // deleteReComment(대댓글 삭제)
  // deleteReComment(Long reCommentId, User user);

}
